import 'package:flutter/material.dart';

class AppColors {
  // --- PALETA CYBER-SANKOFA ---

  // Fundo: Deep Void Purple (O Infinito/Ancestral)
  // Quase preto, mas com um toque de beringela para não ser chapado.
  static const Color background = Color(0xFF120E16); 
  
  // Superfícies (Cards): Um tom acima do fundo para dar profundidade
  static const Color surface = Color(0xFF1E1826);    
  
  // Primária: Cyber Lime (Ação/Tecnologia/Futuro)
  // Usado em botões de ação, timer, conclusões. É a "eletricidade".
  static const Color primary = Color(0xFFCCFF00);    
  
  // Secundária: Digital Terracotta (Alma/Passado/Identidade)
  // Usado em tags de perfil, detalhes humanos, histórico. É o "barro".
  static const Color secondary = Color(0xFFE07A5F);  
  
  // Textos
  static const Color textMain = Color(0xFFF2F0F5);   // Branco Gelo (Off-white)
  static const Color textSub = Color(0xFF9E9CAB);    // Cinza Pedra
  
  // Status
  static const Color success = Color(0xFFCCFF00);    // Mesmo Neon
  static const Color error = Color(0xFFFF453A);      // Vermelho Tech
  static const Color warning = Color(0xFFFFB020);    // Amarelo
}