import 'package:flutter/material.dart';

class AppColors {
  // Fundo: Roxo Meia-Noite Profundo (Tech)
  static const Color background = Color(0xFF1E1B2E); 
  
  // Superfícies (Cards): Um tom ligeiramente mais claro que o fundo
  static const Color surface = Color(0xFF2A273A);    
  
  // Primária: Terracota Vibrante (Alma/Força) - Para botões principais e destaques
  static const Color primary = Color(0xFFE05D39);    
  
  // Secundária: Verde Neon Lima (Energia/Acento) - Para ícones, checks e detalhes
  static const Color secondary = Color(0xFFD2F647);  
  
  // Textos
  static const Color textMain = Color(0xFFFFFFFF);   // Branco puro
  static const Color textSub = Color(0xFFB0AFBC);    // Cinza claro tech
  
  // Status
  static const Color success = Color(0xFFD2F647);    // Neon para sucesso
  static const Color error = Color(0xFFFF453A);      // Vermelho vibrante
  static const Color warning = Color(0xFFFFB020);    // Amarelo alerta
}