name: okan_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  
  cached_network_image: ^3.3.1
  
  # --- TRADUÇÕES ---
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2 # <--- VOLTAMOS PARA A VERSÃO CORRETA AQUI

  # --- VÍDEOS ---
  youtube_player_flutter: ^9.0.1
  video_player: ^2.8.1
  chewie: ^1.7.1

  # --- FIREBASE ---
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.12
  firebase_storage: ^12.4.0
  
  # --- UTILITÁRIOS E UI ---
  image_picker: ^1.1.2
  google_sign_in: ^6.1.0
  fl_chart: ^0.66.0
  url_launcher: ^6.2.0

  # --- GERÊNCIA DE ESTADO E INJEÇÃO ---
  provider: ^6.1.2
  get_it: ^7.7.0
  
  # --- LÓGICA ---
  equatable: ^2.0.5
  uuid: ^4.4.0
  vibration: ^3.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

  # PACOTES DE DESENVOLVIMENTO
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.3.1

flutter:
  uses-material-design: true

vibration: ^1.9.0

# ----------------------------------------------------------
# CONFIGURAÇÕES (TOTALMENTE À ESQUERDA)
# ----------------------------------------------------------

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "flutter_01.png"
  min_sdk_android: 21

flutter_native_splash:
  color: "#F1F5F9"
  image: "flutter_01.png"
  android_12:
    image: "flutter_01.png"
    color: "#F1F5F9"